# ContextCrumb Stats

Run the ContextCrumb stats command and report the result directly:

```powershell
contextcrumb stats
```

For machine-readable output, run:

```powershell
contextcrumb stats --json
```

Do not estimate savings from transcripts. ContextCrumb records direct compression savings in its local JSONL ledger.
